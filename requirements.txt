# VYAAS AI Backend - Railway Deployment Requirements

# Core LiveKit
livekit-agents>=0.8.0
livekit-plugins-google>=0.4.0

# Utilities
python-dotenv
requests
psutil
Pillow

# Note: pyautogui and pyperclip are skipped for cloud deployment
# They require a display/clipboard which cloud servers don't have
